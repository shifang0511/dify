// This file is auto-generated by @hey-api/openapi-ts

import * as z from 'zod'

/**
 * LLMMode
 *
 * Enum class for large language model mode.
 */
export const zLlmMode = z.enum(['chat', 'completion'])

/**
 * ModelConfig
 */
export const zModelConfig = z.object({
  completion_params: z.record(z.string(), z.unknown()).optional(),
  mode: zLlmMode,
  name: z.string(),
  provider: z.string(),
})

/**
 * WorkflowRegenerateNodePayload
 *
 * Payload for per-node refinement of an existing generated graph.
 *
 * The frontend ships the current preview graph alongside the target node
 * id and the user's free-text refinement. Returns the SAME envelope as
 * ``/workflow-generate`` — full graph + errors + repair_attempts — so
 * the frontend can re-use the existing rendering path.
 */
export const zWorkflowRegenerateNodePayload = z.object({
  graph: z.record(z.string(), z.unknown()),
  mode: z.enum(['advanced-chat', 'workflow']),
  model_config: zModelConfig,
  node_id: z.string(),
  refinement: z.string(),
})

export const zPostWorkflowGenerateNodeBody = zWorkflowRegenerateNodePayload

/**
 * Node refinement completed
 */
export const zPostWorkflowGenerateNodeResponse = z.record(z.string(), z.unknown())
